spring:
  application:
    name: patient-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://mridulbindal4_db_user:V248ypNBVyet8FlI@cluster0.4l8zwaj.mongodb.net/patientdb?appName=Cluster0}

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
